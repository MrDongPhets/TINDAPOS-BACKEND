services:
  - type: web
    name: kitapos-backend
    runtime: node
    buildCommand: npm install --include=dev && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
